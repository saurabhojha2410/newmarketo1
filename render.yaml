services:
  - type: web
    name: email-qa-bot
    runtime: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm run start
    envVars:
      - key: NODE_ENV
        value: production
